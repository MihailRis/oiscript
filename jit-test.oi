func square(values):
    x = 0
    while true:
        if x == 5:
            break
        x += 1
    return x

func qsort(values):
    unsorted = true
    while unsorted:
        unsorted = false
        for i : values.len-1:
            if values[i] < values[i+1]:
                tmp = values[i+1]
                values[i+1] = values[i]
                values[i] = tmp
                unsorted = true
    return values